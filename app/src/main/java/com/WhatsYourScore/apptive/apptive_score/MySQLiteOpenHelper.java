package com.WhatsYourScore.apptive.apptive_score;

/**
 * Created by kgm13 on 2016-11-27.
 */
/*
public class MySQLiteOpenHelper extends SQLiteOpenHelper {
    public MySQLiteOpenHelper(Context context, String name, SQLiteDatabase.CursorFactory factory, int version){
        super(context, name, factory, version);
    }

    @Override
    public void onCreate(SQLiteDatabase db) {
        String sql = "create table Sortgrade (" + "_id integer primary key autoincrement, " + "Num_Total int, " + "Num1 int, " +
                "Num2 int, " +"Num3 int, " + "Num4 int);";
        db.execSQL(sql);
    }

    @Override
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {
        String sql = "drop table if exists Sortdata";
        db.execSQL(sql);
        onCreate(db);
    }
}
*/
